# k8s_job.yaml.template
apiVersion: batch/v1
kind: Job
metadata:
  name: file-download-job
spec:
  template:
    spec:
      containers:
      - name: downloader
        image: gcr.io/${GCP_PROJECT_ID}/downloader:latest
      restartPolicy: OnFailure
